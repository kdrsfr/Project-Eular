"""Brute force ile çözüldüğü için verimli değil. Daha iyi bir kodlamayla anında çözülebilir ben şuan ancak 23 saniyede sonuç alabiliyorum."""


number = 5
divisor = 2
while divisor < 21:
    
  if number % divisor == 0:
    divisor += 1
    
  else:
    number += 5
    divisor = 2
      
print (number)
